 
export default (function card() {
        var node = document.createElement("h1");
        var textnode = document.createTextNode("blackout underwater");
        node.appendChild(textnode);  
        document.body.appendChild(node);
}());

